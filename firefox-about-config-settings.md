# Some hidden Firefox about:config settings

## Enable Compact Mode (This is not supported officially.. But still seems to work)
```
browser.compactmode.show: true
```

## Disable site specific zoom levels (zoom in page on single window only)
```
browser.zoom.siteSpecific: false
```
